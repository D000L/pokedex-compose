plugins {
    id 'com.android.library'
    id 'kotlin-android'
    id 'kotlin-kapt'
}

applyAndroidConfig(this)
applyComposeConfig(this)

dependencies {
    api 'androidx.navigation:navigation-compose:2.5.0-beta01'

    implementation "androidx.compose.runtime:runtime:$compose_version"
}
